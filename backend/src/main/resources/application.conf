ktor {
    database {
        url = ${NEST2D_DB_URL}
        name = ${NEST2D_DB_NAME}
        user = ${NEST2D_DB_USER}
        password = ${NEST2D_DB_PASSWORD}
    }
    connector {
        port = 8080
        host = "localhost"
    }
    app {
        version = ${NEST2D_APP_VERSION}
        base_url = ${NEST2D_APP_BASE_URL}
    }
}
